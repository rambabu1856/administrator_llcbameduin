<div class="{{ $grid }}">
  <div class="button-group">

    <button type="{{ $type }}" class="btn {{ $btnClass }}" name="{{ $name }}" id="{{ $name }}"
      data-toggle="tooltip" title="{{ $tooltip }}" data-id= "{{ $dataId }}" style="min-width: 50px;"><i
        class="{{ $faClass }}"></i>{{ $btnText }}</button>

  </div>
</div>
