<div class="{{ $grid }}">
  <div class="form-group">
    <label class = "{{ $lblClass }}" for="{{ $name }}">{{ $lblText }}</label>
    <input type="{{ $type }}" class="form-control form-control-sm" id="{{ $name }}"
      name="{{ $name }}">
  </div>
</div>
